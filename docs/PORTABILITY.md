# Portability gaps — full workflow is not validated

| ID | Status | Exact gap and recovery step |
|---|---|---|
| P01 | PORTABILITY_PENDING | `original_v1/00_scripts` scripts still read derived inputs and write new results through `02_results/` and `tables_for_article/`. `project_config.R` now requires explicit roots, but a per-step input/output binding is not complete. Create a versioned DAG and manifests; do not seed a clean scientific run with an old run's outputs. |
| P02 | BLOCKED_INPUT | Targeted `analyze_organoid.R` expects `organoid_donor_extension/source_data/pseudobulk_metadata.tsv` and `pseudobulk_genes.tsv`. In the inspected local snapshot, CSV counterparts and `pseudobulk_counts.mtx` exist, but the TSV paths do not. Recover authentic TSVs or implement and approve a lossless CSV-to-TSV compatibility test (columns, order, missingness and identifiers); do not silently rename or substitute. |
| P03 | INPUT_BINDING_PENDING | `analysis_plan_locked.json`, `preflight_QC.json`, `conditional_analysis_plan_locked.json`, `analysis_complete.json` and manifests contain stage-specific values/paths. Bind every real file by version, role, bytes and SHA-256, validate patient/sample keys and rebase only reviewed path fields. A saved `status=PASS` is not fresh preflight proof. |
| P04 | PORTABILITY_PENDING | Targeted scripts derive both inputs and outputs from staged `__file__` roots. Scientific execution still needs a reviewed per-step overlay/copy contract. Missing files must stop; the new staging tool does not fabricate these layouts. |
| P05 | INPUT_BINDING_PENDING | `visual_input_snapshot/`, `input_snapshot/`, `source_data/final_panels/`, `current_panel_data/` and `full_source_tables/` are distinct versions/layouts. `docs/source_table_schema_map.csv` compares headers only. It is not a data-equivalence map, and reader Table S6 is not automatically legacy Table_S6. |
| P06 | PORTABILITY_PENDING | Recorded `build_workbook.mjs` requires a legitimately accessible `@oai/artifact-tool` module entry through `ICI_ARTIFACT_TOOL_MODULE`. No private cache path or public installation claim is supplied. A public-tool replacement would require approved cell-by-cell comparison including order, columns, nulls, formula/cached values and layout; that comparison was not run. |
| P07 | FINAL_FIGURE_SOURCE_MAPPING_PENDING | `final_20260917/plot_final.R` is a real recovered 170 mm renderer for Figs 2/3/5/6 only. It does not supply a verified final 170 mm source for Figs 1/4. Recorded main code is 180 mm. Supplementary and final workbook sources are preserved, but complete publication-version/layout equivalence is unverified. |
| P08 | ENVIRONMENT_SETUP_REQUIRED | The configured local Python lacks packages listed in the local dependency report (beautifulsoup4, lxml, openpyxl, reportlab at this inspection). R/Node installed inventories are local evidence only; historical package versions and cross-platform graphics/fonts remain unvalidated. No package installation occurred. |
| P09 | REMOTE_VERSION_NOT_VERIFIED | The Figshare DOI root is author-supplied. Exact public version, downloadable members and matching hashes remain unverified; no network request or automatic data download was made. |
| P10 | BLOCKED_INPUT | `ICI_HISTORICAL_DISPLAY_LABELS`, `ICI_HISTORICAL_ANNOTATIONS` and `ICI_RECEPTOR_MAP` must identify versioned external metadata. Observed statistics/sample counts, gate/claim decisions and fixed receptor membership were removed from code literals. Recover their original values from the retained local source/approved tables without inventing replacements; see `external_annotation_requirements.csv`. These are display/reference inputs, not permission to rerun or reselect. |
| P11 | INPUT_BINDING_PENDING | Explicit `ICI_ORGANOID_COMPLETE`, `ICI_ORGANOID_COMPARISON_GSEA`, `ICI_RECORDED_ORGANOID_RESULTS` replace latest-folder/timestamp guesses. The historical comparison read in `finalize_organoid.py` remains a historical compatibility check and must not be inserted into a fresh raw-to-final analysis as prior-run reuse. |
| P12 | FINAL_TABLE_SOURCE_MAPPING_PENDING | `plotting/tables/final_20260917/finalize_workbook.py` requires the exact prior workbook, `artifact_authored_cell_changes.json` and `artifact_authored_intermediate.xlsx`. It performs table OOXML transfer/verification, not table inference. Those external inputs and final layout replay were not supplied to or executed by this build. |
| P13 | PUBLICATION_RIGHTS_PENDING | No standalone licence or verified software-author/contact metadata came with code.zip. Confirm ownership/licence and any actual existing licence before remote publication; do not invent a DOI or publication status. |
| P14 | INPUT_SCHEMA_PENDING | Read-only comparison found identical historical up/down/core members, but the core's empty `down_genes` field in original Table_S25 is literal `NA` in Source_025. A naive semicolon split interprets this as one member. Declare and test missing-value semantics before any CSV/TSV bridge; do not silently treat the two files as interchangeable. |

No item above establishes that the article's statistical results are wrong. The restrictions describe portability and evidence limits for this source delivery. Old scripts with network operations, Windows recovery code and archived numerical verifiers are retained for provenance, not automatically executed.
