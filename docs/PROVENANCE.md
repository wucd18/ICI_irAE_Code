# Source identity and adaptations

The separately supplied code.zip and the execution package's embedded code.zip matched SHA-256 `a56f2861c233aaec01867d430935c840cd29e631deb9c20fde9b0f0a723da468`. The archive has 125 non-directory files. Independent rechecking matched the 87-entry recovery manifest and all 106 index records. Nineteen index paths are relocated files with matching bytes in `recorded_CLOSEOUT_presentation/`, not nineteen lost programs.

All 68 initially classified scientific/preparation/plotting/support sources are retained by identity. Additional genuine local final figure/table programs and necessary recorded workbook preparation were recovered; the mixed visual preparer was reduced to a tested input-copy/alias module. The exact current index is `script_index.csv`: `sha256` means the original bytes; `adapted_sha256` means the delivered bytes. Rows marked extraction are subsets, not claimed byte-identical originals.

Unmodified originals, source disposition lists, exact unified diffs and execution logs are kept in the separate LOCAL_REVIEW_ONLY delivery directory. Only this one explained source tree belongs in Git. Path/config edits remove author machine drives and cache paths where adapted; explicit archived annotation/reference inputs replace embedded result literals. Statistical parameters, model functions, FDR gates and seeds were not tuned, and no scientific analysis was executed.

The supplied source package is not an established complete final publication source set. Code identity, portable execution, input validity, numerical reproduction and final layout identity are separate questions. No old scientific output, approved figure, manuscript, source-data file or remote repository was modified.
